# Local Config

Put local-only API overrides in this directory.

- `appsettings.local.json` is loaded after `api/appsettings.json`.
- Commit examples, not local secrets.
- The checked-in auth/token keys are local development defaults only.
